package frc2713.robot.subsystems.led;

/** Logical LED states, ordered by conceptual priority (highest first). */
public enum LEDState {
  PROBLEM,
  STOP_IT,
  ENDGAME_PANIC,
  LAUNCHING,
  INTAKING,
  DETECTING,
  PARTY,
  ACTIVE,
  NOT_ACTIVE
}
